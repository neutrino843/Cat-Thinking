# 猫思之 · 产品需求文档（PRD）

| 项 | 内容 |
| --- | --- |
| 产品名 | 猫思之（MaoSiZhi） |
| 版本 | v0.1（MVP 规划） |
| 日期 | 2026-09-21 |
| 状态 | 已评审待开发 |
| 一句话 | 本地优先、无账号、手绘温暖风的思维导图 + 轻量甘特图工具 |

---

## 1. 产品概述

### 1.1 定位

猫思之是一款**本地优先**的思考工具：用一张手绘感的画布帮用户"想清楚"（思维导图），再把任意节点"排得开"（甘特图）。数据只存在用户自己的设备里，无需注册、无需联网。

### 1.2 差异化

1. **本地优先**：无账号、无云端、无强制遥测；数据可随时完整导出迁移，无锁定。
2. **温暖手绘**：奶油纸色、蜡笔笔触、轻微抖动线条，降低"工具感"，让思考像涂鸦。
3. **双视图同源**：思维导图节点即甘特任务，一个数据模型两个视角，杜绝两处维护。
4. **轻快**：秒开、低内存、离线可用；不做重型项目管理。

### 1.3 目标用户与核心场景

| 用户 | 场景 |
| --- | --- |
| 学生 / 自学者 | 读书笔记、复习提纲、学习计划 |
| 独立开发者 / 自由职业者 | 项目规划、功能拆解、排期跟踪 |
| 小团队（1–5 人） | 头脑风暴、会议纪要、轻量任务分工 |

典型故事：
- 小林读完一章书，用"读书笔记"模板建导图，Tab/Enter 快速录入，导出 PNG 贴进笔记。
- 阿伟规划独立开发项目：导图拆解功能后，在甘特视图里给节点设日期和依赖，拖拽调整排期。
- 周会纪要现场投屏，用演示模式按分支逐级讲解，会后导出 PDF 归档。

### 1.4 非目标（明确不做）

- 多人实时协作、云同步、团队权限管理。
- 完整项目管理：资源均衡、成本核算、燃尽图。
- 插件市场（MVP 后评估）。

---

## 2. 设计原则

- **P1 数据属于用户**：本地存储、完整导出、格式开放、无供应商锁定。
- **P2 零门槛**：打开即用、无注册、离线可用、有引导文档。
- **P3 温暖但可读**：手绘感服务于氛围；正文可读性永远优先于风格。
- **P4 键盘友好**：核心操作全部有快捷键，支持全键盘导航。
- **P5 渐进复杂**：默认轻量；甘特、样式等能力按需浮现，不轰炸新手。

---

## 3. 术语表

| 术语 | 含义 |
| --- | --- |
| 节点 / 分支 | 导图基本单元；一级子节点称为分支 |
| 关系线 | 任意两节点间的箭头连接（P1） |
| 概要 / 边界 | 框住一组节点的括号 / 矩形（P1） |
| 大纲视图 | 树形文本列表，与导图双向同步 |
| 文档库 | 应用内 IndexedDB 管理的多文档列表 |
| 任务 | 拥有日期字段的导图节点（甘特同源） |
| 依赖 | 任务间前置关系：FS/SS/FF/SF |
| 手绘程度 | 笔触抖动强度：简洁 / 中等 / 很手绘 |

---

## 4. 功能需求

优先级：**P0 = MVP 必须，P1 = 重要但可后置，P2 = 远期**。

### 4.1 思维导图核心

#### 4.1.1 无限画布（P0）
- 滚轮缩放（15%–300%），以光标为中心；触控板捏合（P1）。
- 平移：空白处拖拽 / 中键拖拽 / 空格+拖拽。
- 「适应窗口」按钮与快捷键，一键将内容居中适配视口。

#### 4.1.2 节点操作（P0）
- 增加：`Tab` 加子节点、`Enter` 加兄弟节点，新增后立即进入编辑。
- 编辑：双击 / 单击已选中节点 / `F2`；`Enter` 确认、`Esc` 取消。
- 删除：`Delete` 删除选中节点及其子树（根节点不可删）。
- 移动：拖拽节点到另一节点上换父；`Alt+↑/↓` 在兄弟间排序。
- 多选：Ctrl/Shift 点击；支持批量删除、复制。
- 剪贴板：`Ctrl+C/X/V` 应用内复制粘贴子树（粘贴自动重映射 id）。
- 撤销重做：`Ctrl+Z` / `Ctrl+Shift+Z`（或 `Ctrl+Y`），≥100 步。
- 折叠展开：`Space` 切换；折叠节点显示 `+N` 汇总角标。

**验收**：连续完成 建子→改文本→拖拽换父→删除→撤销×4，画布与大纲均恢复一致。

#### 4.1.3 结构布局（P0 两种，其余后置）
- P0：逻辑图（右侧展开）、树形图（左右平衡）。
- P1：组织架构图、括号图；P2：鱼骨图、时间轴、自由布局。
- 布局为实时自动计算；切换布局保留结构与样式。

#### 4.1.4 节点内容
- P0：文本。
- P1：备注（角标 ✎，悬浮/侧栏查看）、超链接、标签、分支配色、图标。
- P2：图片、附件、代码块、自动编号。

#### 4.1.5 关系表达
- P1：关系线（箭头+文字）、概要（花括号）、边界框。
- P2：标注、便签。

#### 4.1.6 大纲视图（P0，M3）
- 右侧面板树形列表，与导图**双向实时同步**。
- `Enter` 建兄弟、`Tab` 建子节点，适合快速录入。
- P2：大纲内拖拽排序。

#### 4.1.7 搜索与过滤（P0 基础）
- `Ctrl+F` 按文本搜索，命中节点高亮，回车循环跳转。
- P1：按标签/状态/负责人过滤；仅显示命中分支。

#### 4.1.8 演示模式（P2，M5 后）
- 全屏按分支逐级展开讲解，`←/→` 翻页。

#### 4.1.9 模板库（P0 轻量）
- 内置：欢迎教程、读书笔记、项目规划、会议纪要、学习计划、SWOT。
- 新建文档时可选模板；P1 支持把当前文档存为模板。

### 4.2 甘特图（与导图同源）

**核心概念：没有独立任务实体——带日期字段的导图节点就是任务。** 在甘特视图中改动会写回节点，反之亦然。

| 能力 | 优先级 | 说明 |
| --- | --- | --- |
| 任务字段 | P0 | 开始/结束日期、进度%；P1 加优先级、负责人、标签、备注 |
| 依赖关系 | P0 FS | 前置任务+箭头；P1 扩展 SS/FF/SF 与冲突提示 |
| 里程碑 | P0 | 菱形样式，零工期 |
| 今日线 / 周末 | P0 | 今日竖线、周末底纹；节假日配置 P2 |
| 拖拽 | P0 | 拖动条整体平移，拖边缘改工期 |
| 时间轴缩放 | P0 | 日 / 周 / 月 |
| 关键路径 | P2 | 计算并高亮 |
| 基线对比 | P2 | 保存基线快照 |
| 其他视图 | P1 表格 | P2 看板 / 日历 / 资源负载 |
| 导出 | P0 | 随文档导出 PNG/SVG；P1 CSV |

**MVP 裁剪**：日期 + 进度 + FS 依赖 + 拖拽 + 今日线即可上线，不必做完整项目管理。

**验收**：导图中给 3 个兄弟节点设日期与 FS 依赖 → 甘特视图正确绘制箭头；拖动下游任务条，日期写回导图节点；撤销后两端一致。

### 4.3 本地数据与文件管理

| 能力 | 优先级 | 说明 |
| --- | --- | --- |
| 存储方案 | P0 | IndexedDB（Dexie）；P2 桌面端 Tauri + SQLite |
| 应用内文档库 | P0 | 多文档、按更新时间排序、重命名、删除 |
| 自动保存 | P0 | 变更后 ≤1s 防抖落盘；关闭/隐藏页面前强制 flush |
| 崩溃恢复 | P0 | 重启打开最近文档，内容为最近一次落盘状态 |
| 回收站 | P1 | 删除文档进入回收站，可恢复 |
| 版本历史 / 快照 | P2 | 手动快照 + 自动每日快照 |
| 导入导出 | P0 | JSON（无损全量）、PNG、SVG；P1 Markdown/OPML/CSV/PDF；P2 XMind/FreeMind |
| 单文件模式 | P1 | File System Access API 直接读写 .msz 文件 |
| 本地加密 | P2 | WebCrypto AES-GCM，密码保护文档 |
| 备份提醒 | P1 | 距上次导出超 N 天时温和提示 |
| 隐私 | P0 | 无账号、无遥测；运行期零网络请求；字体/图标全部本地 |

**验收**：新建→输入→直接关闭标签页→重开浏览器，文档完整恢复；导出 JSON 再导入，结构与样式一致。

### 4.4 交互与效率

- 命令面板（P0）：`Ctrl/Cmd+K` 模糊搜索命令，方向键+回车执行。
- 自动整理（P0）：布局实时自动；`适应窗口` 一键归位。对齐吸附/辅助线 P1。
- 多文档标签页 P1；收藏/文件夹/标签管理 P2。
- 批量操作 P1：多选批量改色、加标签。
- 新手引导（P0）：首次打开自动载入「欢迎」导图（含快捷键卡）。
- 可访问性（P1）：全键盘可达、文本对比度 ≥ 4.5:1、`prefers-reduced-motion` 与手动"减少动效"开关、语义化 aria。
- 国际化（P1）：zh-CN 默认，文案集中管理，预留 en-US。

### 4.5 视觉：温暖手绘

**亮色 token**

| Token | 值 | 用途 |
| --- | --- | --- |
| paper | `#FAF6EE` | 画布纸底 |
| panel | `#F3ECDD` | 面板底 |
| ink | `#3F3A33` | 主文字/线条 |
| ink-soft | `#8B8579` | 次级线条 |
| accent（陶土橙） | `#C96F4A` | 根节点/主操作 |
| sage | `#8A9B6E` | 分支色 |
| mustard | `#D9A441` | 分支色 |
| 分支色板 | 橙/绿/黄/雾蓝 `#7F9BB3`/玫瑰 `#B5838D`/藕紫 `#9A8C98` | 按分支轮转 |

**暗色 token**（温暖暗，非纯黑）：paper `#2A2622`、panel `#33302B`、ink `#E8E0D2`、分支色提亮降饱和（如 `#E08B66` / `#A8B98A` / `#E6BC5E`）。

- 字体：标题/品牌用手写感栈（`Segoe Print / Comic Sans MS / 楷体`），**正文与节点文本用高可读系统字体**，避免全篇手写难读。
- 纹理：细网格 + 轻噪点纸面（P1 加噪点滤镜）。
- 线条：圆头、种子随机抖动（同一路径每次渲染抖动一致）、双描线模拟铅笔复写；自实现 Rough.js 思路的 sketch 路径生成器，大图性能不足时切 Canvas。
- 动效：克制的节点生长/展开过渡；全部可关（respect reduced motion）。
- **手绘程度滑杆**：简洁（直线无抖动）/ 中等 / 很手绘，全局实时生效。

**验收**：同一文档在滑杆三档下切换，节点几何位置不变；暗色模式下无纯黑背景与低对比文字。

---

## 5. 非功能需求

| 维度 | 指标 |
| --- | --- |
| 性能 | 1000 节点：缩放/拖拽 ≥ 45fps；首屏可交互 < 2s；保存操作无感知（<50ms 主线程占用）；内存 < 300MB |
| 兼容 | Chrome/Edge ≥ 100（P0），Firefox/Safari 尽力（P1）；Win/macOS/Linux；PWA 可安装 P1，Tauri 桌面 P2 |
| 可靠性 | 断电丢失 ≤ 最近 1s 变更；数据格式带 `version` 字段向后兼容；自动保存失败有醒目提示 |
| 安全 | 无远程代码加载；导入文件做结构校验；严格 CSP |
| 合规 | 开源发布（协议 TBD，倾向 MIT/AGPL）；字体用系统栈或开源字体规避版权；提供隐私说明 |

---

## 6. 技术架构

### 6.1 选型

| 层 | 选型 | 理由 |
| --- | --- | --- |
| 框架 | React 18 + TypeScript + Vite | 生态成熟、类型安全、秒级冷启 |
| 状态 | Zustand + 不可变快照历史（命令模式语义） | 撤销重做简单可靠；动作即命令 |
| 渲染 | SVG 为主 + 自研 sketch 路径生成器 | 文本交互/导出天然友好；>1500 节点再引入视口裁剪或 Canvas 图层 |
| 存储 | Dexie / IndexedDB | 事务性、容量大、无依赖后端 |
| 桌面 | Tauri（P2） | 比 Electron 轻一个量级，契合本地工具 |
| 测试 | Vitest（模型/布局）+ Playwright（保存恢复、导入导出 E2E） | 数据安全是生命线，重点测 |

### 6.2 数据模型草案

```jsonc
{
  "version": 1,
  "id": "uuid",
  "title": "项目规划",
  "rootId": "n1",
  "layout": "logic",            // logic | tree | org ...
  "nodes": {
    "n1": { "id": "n1", "parent": null, "children": ["n2","n3"], "text": "中心主题" },
    "n2": { "id": "n2", "parent": "n1", "children": [], "text": "需求梳理",
            "note": "备注", "href": "https://…", "color": "b0", "collapsed": false,
            "task": { "start": "2026-09-21", "end": "2026-09-30", "progress": 0.4,
                      "deps": [{ "from": "n3", "type": "FS" }] } }
  },
  "createdAt": 1758400000000, "updatedAt": 1758400000000
}
```

### 6.3 目录结构（MVP）

```
src/
  types.ts            # DocData / MindNodeData
  lib/                # sketch 手绘路径 / layout 布局 / theme / exporters
  store/              # docStore(Zustand+历史) / settings / db(Dexie) / persist
  data/templates.ts   # 模板
  components/         # Canvas / Toolbar / Sidebar / Outline / Palette
```

### 6.4 关键决策记录

- **ADR-1 SVG 优先**：节点数 <1500 时 SVG 文本选择、无障碍与导出成本最低；性能兜底方案是视口裁剪与 Canvas 图层，不预先过度设计。
- **ADR-2 快照式撤销**：以动作粒度保存 nodes 引用快照（不可变更新使旧引用天然共享），内存开销可控、正确性远高于 diff 补丁；文本输入合并为一次快照。
- **ADR-3 手绘笔触自研**：只用到 Rough.js 的抖动思路，自实现 ~100 行种子随机路径生成器即可，避免 40KB 依赖并保证种子稳定（防重渲染闪烁）。
- **ADR-4 甘特同源**：任务字段内嵌节点（`task` 字段），不建平行任务表，从模型上保证双视图永不失同步。

---

## 7. MVP 范围与里程碑

| 里程碑 | 范围 | 验收要点 |
| --- | --- | --- |
| **M1 画布与节点** | 无限画布、节点增删改/拖拽换父/折叠、逻辑+树形布局、撤销重做、核心快捷键 | §4.1.2 验收；1000 节点流畅 |
| **M2 本地数据** | Dexie 文档库、自动保存、崩溃恢复、JSON/SVG/PNG 导出、JSON 导入 | §4.3 验收 |
| **M3 大纲与效率** | 大纲双向同步、Ctrl+F 搜索、快捷键完备、命令面板 Ctrl+K、手绘程度三档 | 导图/大纲互改 10 轮一致 |
| **M4 甘特基础** | 日期/进度/FS 依赖/拖拽/今日线/里程碑，与导图同源 | §4.2 验收 |
| **M5 主题与演示** | 手绘主题打磨、模板库、演示模式 | §4.5 验收 |

MVP 后：版本历史、回收站、加密、看板/日历、关系线/概要、Markdown/OPML、Tauri、单文件模式、插件。

---

## 8. 风险与对策

| 风险 | 对策 |
| --- | --- |
| 手绘渲染在大图上卡顿 | 种子缓存路径字符串；视口裁剪；必要时 Canvas 图层渲染 |
| 中文手写字体体积过大 | 标题用系统手写栈，正文用系统高可读字体，零下载 |
| IndexedDB 可用性（隐私模式等） | 降级 localStorage 提示 + 强化导出备份提醒 |
| 范围蔓延 | MVP 冻结为 M1–M5；新需求一律进 Backlog 评审 |
| SVG 文本测量与 DOM 渲染偏差 | 统一 Canvas measureText 近似测量 + 最小宽度兜底 |

---

## 9. 附录

### A. 快捷键表（P0）

| 快捷键 | 功能 | 快捷键 | 功能 |
| --- | --- | --- | --- |
| Tab | 新建子节点 | Enter | 新建兄弟节点 / 确认编辑 |
| Delete / Backspace | 删除节点 | Space | 折叠/展开 |
| F2 / 双击 | 编辑节点 | Alt+↑/↓ | 兄弟排序 |
| ↑↓←→ | 节点导航 | Ctrl+Z / Ctrl+Shift+Z | 撤销 / 重做 |
| Ctrl+C / X / V | 复制 / 剪切 / 粘贴 | Ctrl+F | 搜索 |
| Ctrl+K | 命令面板 | — | — |

### B. 亮暗主题 token
见 §4.5；所有颜色以 CSS 变量 + 渲染层 token 双轨管理（UI 用 CSS 变量，SVG 用 JS token，保证导出内联样式正确）。

### C. 甘特任务字段（M4）

`task.start / task.end / task.progress / task.deps[{from,type=FS}] / task.milestone`，完整字段表见 §4.2。
